{
  "name": "pocket-dev-relay",
  "version": "0.1.0",
  "description": "スマホからPCのターミナルにアクセスするリモート開発ツール",
  "main": "dist/server.js",
  "bin": {
    "pocket-dev-relay": "dist/cli.js"
  },
  "files": [
    "dist/",
    "public/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "npm run build:server && npm run build:client && npm run build:cli",
    "build:server": "esbuild src/server.ts --bundle --platform=node --outfile=dist/server.js --external:node-pty",
    "build:client": "esbuild src/client/app.ts --bundle --minify --outfile=public/app.js",
    "build:cli": "esbuild src/cli.ts --bundle --platform=node --outfile=dist/cli.js --external:node-pty --banner:js='#!/usr/bin/env node'",
    "dev": "npm run build && node dist/server.js",
    "start": "node dist/server.js",
    "start:legacy": "node server.js",
    "start:awake": "caffeinate -dimsu node dist/server.js",
    "watch": "concurrently \"npm run watch:server\" \"npm run watch:client\"",
    "watch:server": "esbuild src/server.ts --bundle --platform=node --outfile=dist/server.js --external:node-pty --watch",
    "watch:client": "esbuild src/client/app.ts --bundle --outfile=public/app.js --watch",
    "lint": "eslint .",
    "format": "prettier --write .",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.client.json --noEmit",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run"
  },
  "keywords": [
    "terminal",
    "remote",
    "mobile",
    "development",
    "websocket",
    "pty"
  ],
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.0",
    "@vitest/ui": "^4.0.18",
    "concurrently": "^9.0.0",
    "esbuild": "^0.24.0",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "typescript": "^5.6.0",
    "vitest": "^4.0.18"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Y-Kanekoo/pocket-dev-relay.git"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "nanoid": "^5.0.5",
    "node-pty": "^1.0.0",
    "qrcode": "^1.5.3",
    "ws": "^8.17.0",
    "xterm": "^5.3.0",
    "xterm-addon-fit": "^0.8.0"
  }
}
